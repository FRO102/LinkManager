services:
  link-manager:
    build: .
    container_name: link-manager
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3000
      - DATA_DIR=/app/data
      # - BACKUP_RETENTION=14           # nº de backups diários a manter (default: 14)
      # - LINK_CHECK_INTERVAL_HOURS=24  # frequência da verificação automática de links (default: 24h)
